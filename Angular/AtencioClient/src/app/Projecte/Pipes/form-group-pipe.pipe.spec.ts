import { AsFormGroupPipe } from './form-group-pipe.pipe';

describe('FormGroupPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AsFormGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
